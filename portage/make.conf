# NOTE: This stage was built with the bindist Use flag enabled

LC_MESSAGES=C.utf8
COMMON_FLAGS="-O2 -pipe -march=native"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

USE="wayland X -sendmail pulseaudio gtk4 -gpm  -gtk-doc vaapi pipewire"
MULTILIB_ABIS="amd64"
ACCEPT_LICENSE="*"
L10N="en en-US zh-CN"
#VIDEO_CARDS="amdgpu radeonsi i915"
#ALSA_CARDS="maestro3 hdsp hdspm ymfpci"
KCFLAGS="-march=native -pipe"

# CPU_FLAGS_X86 can be generated by cpuid2flags:
# echo "*/* $(cpuid2cpuflags)" > /etc/portage/package.use/00cpu-flags

FEATURES="binpkg-request-signature"
# EMERGE_DEFAULT_OPTS="${EMERGE_DEFAULT_OPTS} --getbinpkg"

GENTOO_MIRRORS="https://mirrors.neusoft.edu.cn/gentoo"

# FETCHCOMMAND="aria2c --conf-path=/etc/portage/aria2.conf -d \"\${DISTDIR}\" -o \"\${FILE}\" \"\${URI}\""
